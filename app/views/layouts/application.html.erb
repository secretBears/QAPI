<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
      <div class="fullWidth">
        <aside class="large-2 columns">
          <header>
            <h1>QAPI</h1>
            <h2>by secretBears</h2>
          </header>
          <%= render partial: "admin_menu" if can? :manage, :all %>
        </aside>

        <section id="content" class="large-10 columns">
          <header class="row">
            <% if current_user %>
              <%= render partial: "controls" %>
            <% else %>
                <ul class="button-group">
                  <li>
                    <a href="http://github.com/secretBears/QAPI" class="button">View on GitHub</a>
                  </li>
                  <li>
                    <a href="http://github.com/secretBears/QAPI" class="button">Info</a>
                  </li>
                </ul>
            <% end %>
          </header>

          <%= yield %>
        </section>
      </div>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </body>
</html>