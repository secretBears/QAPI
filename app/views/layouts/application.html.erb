<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
      <div class="fullWidth">
        <aside class="large-2 columns">
          <header>
            <h1>QAPI</h1>
            <h2>by secretBears</h2>
          </header>
        </aside>

        <section id="content" class="large-10 columns">
            <% if current_user %>
              <header class="row">
                <%= render partial: "controls" %>
              </header>
            <% else %>
              <%= link_to "Get your free key", new_user_registration_path, class: "button" %>
              <%= link_to "Have an account? Log in", new_user_session_path, class: "button" %>
            <% end %>
          <%= yield %>
        </section>
      </div>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </body>
</html>