<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>

    <%= csrf_meta_tags %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </head>

  <body>
    <div class="fullWidth">
      <aside class="large-2 columns">
        <header>
          <h1>QAPI</h1>
          <h2>by secretBears</h2>
        </header>

        <%= render partial: "menu" %>
      </aside>

      <section id="content" class="large-10 columns">
        <header class="row">
          <h1 id="page-title" class="large-8 columns"><%= yield :page_title %></h1>

          <nav class="large-2 columns">
            <a href="#" data-dropdown="drop1" class="button expand dropdown">Settings</a><br>
            <ul id="drop1" data-dropdown-content class="f-dropdown">
              <li><a href="#">This is a link</a></li>
              <li><a href="#">This is another</a></li>
              <li><a href="#">Yet another</a></li>
            </ul>
          </nav>

          <nav class="large-2 columns">
            <%= button_to "Logout", destroy_admin_session_path, class: "button expand alert", method: :delete %>
          </nav>
        </header>

        <%= yield %>
      </section>
    </div>

    <%= yield :after_scripts %>
  </body>
</html>